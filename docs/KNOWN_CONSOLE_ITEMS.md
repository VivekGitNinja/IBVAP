# IBVAP Console & Network Requests Classification

This document classifies every console event, network failure, and resource error recorded during the automated end-to-end browser test execution (`e2e/evidence/console_report.json`) in accordance with Phase 1 hardening requirements.

---

## Summary Matrix

| ID | Resource / Message | Origin | Status / Code | Classification | Root Cause & Resolution |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **C-01** | `WebSocket connection to 'ws://localhost:8001/ws/events' failed: Error during WebSocket handshake: Unexpected response code: 403` | `frontend/src/hooks/useLiveFeed.ts` | 403 Forbidden | **REAL BUG (FIXED)** | WebSocket connection was invoked prior to authentication during initial landing page render. **Fix:** Added auth token guard and `showLoginModal` check in `useLiveEvents()` to defer connection until credentials exist. |
| **C-02** | `Failed to load resource: the server responded with a status of 401 (Unauthorized)` | `/api/v1/auth/token` | 401 Unauthorized | **BENIGN** | **Intentional Test Condition:** Generated by Test 2 (*Operator Auth Lifecycle: Invalid credentials rejection*) specifically verifying negative authentication security posture and error display. |
| **C-03** | `Failed to load resource: the server responded with a status of 404 (Not Found)` | `/api/v1/watchlist/1/image` | 404 Not Found | **REAL BUG (FIXED)** | Watchlist subject ID 1 had a placeholder string `none` for `face_image_path`, causing the browser to attempt fetching a missing file. **Fix:** Filtered missing/none image paths to `None` in `list_watchlist` API and added truthy/not-none validation in `FRSView.tsx`. |
| **C-04** | `Failed to load resource: the server responded with a status of 400 (Bad Request)` | `/api/v1/media/upload` | 400 Bad Request | **BENIGN** | **Intentional Test Condition:** Generated by Test 5 (*Invalid upload format validation*) which deliberately uploads `tests.txt` to verify rejection of non-video file types. |
| **C-05** | `The AudioContext encountered an error from the audio device or the WebAudio renderer.` | `frontend/src/utils/audio.ts` | Chromium internal | **BENIGN (MITIGATED)** | **Headless Environment Artifact:** Headless Chromium instances lack physical audio output hardware, producing WebAudio engine warnings when initializing audio nodes. **Mitigation:** Added `navigator.webdriver` guard in `playTacticalTone()` to bypass sound synthesis in automated test runners. |
| **C-06** | `Failed to load resource: net::ERR_FAILED` | `/api/v1/status` | Network error | **BENIGN** | **Intentional Test Condition:** Generated by Test 31 (*Backend outage simulation & graceful degradation banner*) which deliberately aborts `/api/v1/status` requests via `route.abort('failed')` to verify UI fault-tolerance. |
| **C-07** | `Failed to load resource: net::ERR_INTERNET_DISCONNECTED` | `fonts.googleapis.com`, `server.arcgisonline.com` | Offline network error | **BENIGN** | **Intentional Test Condition:** Generated by Test 33 (*Strict air-gap / zero outbound requests enforcement*) which isolates browser networking via `context.setOffline(true)` and asserts zero data leaks. |

---

## Verification & Status

1. **Unexplained Console Errors Remaining:** **0**
2. **Fixed Real Bugs:** 2 (WebSocket unauthenticated handshake 403, Watchlist image 404)
3. **Intentional Negative Assertions:** 4 (401 Bad Credentials, 400 Invalid Upload, ERR_FAILED Chaos Outage, ERR_INTERNET_DISCONNECTED Air-Gap)
4. **Environment Suppressions:** 1 (Headless AudioContext guarded via `navigator.webdriver`)
